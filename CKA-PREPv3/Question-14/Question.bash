# CKA Practice Lab - Question 14: Find out Cluster Information
#
# Tasks:
# 1. Find and record the number of controlplane nodes in the Kubernetes cluster.
# 2. Find and record the number of worker (non-controlplane) nodes in the cluster.
# 3. Determine the cluster's Service CIDR.
# 4. Find out which CNI plugin is configured and note its config file path.
# 5. Find the static pod name suffix for static pods running on controlplane nodes.
#
# Record the answers in this format at /opt/course/14/cluster-info:
# 1: [CONTROLPLANE_COUNT]
# 2: [WORKER_COUNT]
# 3: [SERVICE_CIDR]
# 4: [CNI_PLUGIN_NAME], [CNI_PLUGIN_CONFIG_PATH]
# 5: [STATIC_POD_SUFFIX]
#
