# Question 16 | Update CoreDNS Configuration

# Task
# 1. Backup current CoreDNS ConfigMap YAML to /opt/course/16/coredns_backup.yaml
# 2. Update CoreDNS so SERVICE.NAMESPACE.custom-domain resolves like
#    SERVICE.NAMESPACE.cluster.local
# 3. Ensure both domains work after change

# Target ConfigMap:
# kube-system/coredns
