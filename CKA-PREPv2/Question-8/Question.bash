# Question 8 | Upgrade Node and Join Cluster

# Task
# 1. Match worker Kubernetes version to the control plane version.
# 2. Join the worker node to the cluster using kubeadm.

# Notes
# - You can create and save the join command to /opt/course/8/join-command (optional).
# - Validation checks that the cluster has at least one Ready worker and all Ready workers
#   match the control plane version.
