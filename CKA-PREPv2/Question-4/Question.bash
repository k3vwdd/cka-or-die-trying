# Question 4 | Pods Terminated First

# Task
# Find Pods in namespace project-c13 that would be terminated first under resource pressure.
# Write the pod names (one per line) to:
# /opt/course/4/pods-terminated-first.txt

# Hint
# Pods with QoS class BestEffort are terminated first.
