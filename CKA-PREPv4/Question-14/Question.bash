Question 14 | Controlplane-Only Scheduling

Create pod cp-only in namespace default with image httpd:2-alpine.

Requirements:
1. Pod must schedule only onto controlplane nodes
2. Do not modify node labels or taints
3. Pod must become Running
