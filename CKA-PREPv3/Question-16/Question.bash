Question 16 | Namespaces and API Resources

1. Create the output directory if needed:
   /opt/course/16

2. Write all namespaced Kubernetes API resource names into:
   /opt/course/16/resources.txt

3. Find the namespace matching:
   project-*
   that has the highest number of Role resources.

4. Write the result into:
   /opt/course/16/crowded-namespace.txt

5. The crowded namespace output should contain:
   - namespace name
   - number of roles

6. Use only the Kubernetes cluster state to determine the answer.
