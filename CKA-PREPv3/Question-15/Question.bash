#!/bin/bash
# Question 15 | Cluster Event Logging

# Complete the following tasks:
# 1. Write a kubectl command into /opt/course/15/cluster_events.sh
#    that lists latest cluster-wide events sorted by metadata.creationTimestamp
# 2. Delete the kube-proxy Pod and write the related events to /opt/course/15/pod_kill.log
# 3. Manually kill the kube-proxy container via container runtime and write resulting events to /opt/course/15/container_kill.log
