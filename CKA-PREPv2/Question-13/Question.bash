# Question 13 | Gateway API HTTPRoute

# Task
# In namespace project-r500 and using existing Gateway 'main':
# 1. Create HTTPRoute traffic-director that replicates /desktop and /mobile routing
# 2. Extend with /auto path:
#    - if User-Agent exactly 'mobile' -> web-mobile
#    - otherwise -> web-desktop

# Legacy ingress reference:
# /opt/course/13/ingress.yaml
