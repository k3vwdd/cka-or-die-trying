# Task: Fix Kubelet on controlplane node
# 1. Investigate why kubelet is not running on controlplane and fix the issue so the node becomes Ready.
# 2. Then create a Pod named 'success' in namespace 'default' using image nginx:1-alpine.
